
enum RecordingButtonStatus {
  BEFORE_RECORDING,
  DURING_RECORDING,
  AFTER_RECORDING,
}

enum GroupEditMenu {
  EDIT,
  LEAVE,
}

enum LoginScreenStatus {
  SIGNED_UP,
  SIGNED_IN,
  FAILED,
}

enum RecordingOpenMode {
  FROM_HOME,
  FROM_GROUP,
}
